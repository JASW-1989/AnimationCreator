# 0.6.0-alpha / Atelier

See docs/AUDIT-IMPLEMENTATION-MATRIX.json for all 26 audited conclusions.

Five drawing planes; revision-aware native seed paint; safe split/merge handling; authored shading dependencies; ten-stage review DAG; named references/parts/anchors; conservative protected scopes; actual image MCP; editing leases; measured trial logs. New commands and old prod_* edits share invalidation.

24 typed authoring commands, 16 MCP tools total. No model API, subscription or runtime package install.

Browser localhost navigation is policy-blocked in the evaluation environment. The new integration suite explicitly records its test-only transport bridge to real HTTP. It does not claim live Codex or native same-origin browser acceptance.

Artwork remains an authored/native functional study, not animation-film-grade assets. Read Guide.zh-TW.html for numerical and workflow limitations.
